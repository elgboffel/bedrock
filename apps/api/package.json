{
  "name": "api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"tsdown --watch\" \"nodemon dist/index.mjs\"",
    "build": "tsdown",
    "start": "node dist/index.mjs",
    "lint": "biome lint .",
    "typecheck": "tsc --noEmit",
    "format": "biome format ."
  },
  "dependencies": {
    "@repo/common": "workspace:*",
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^20.0.0",
    "tsdown": "^0.20.3",
    "typescript": "^5.9.3"
  }
}
